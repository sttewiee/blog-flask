events { worker_connections 1024; }
http {
    server {
        listen 80;
        # ВАЖНО: Вставь сюда свой домен вручную
        server_name lapikoff.duckdns.org; 

        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }
        location / {
            return 404;
        }
    }
}