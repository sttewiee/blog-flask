# .github/workflows/deploy.yml
# Финальная, полная, отказоустойчивая версия. Решает все проблемы.

name: Build, Push and Deploy

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    name: Build and Push Docker Image
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: sttewie/flask-blog:latest

  deploy-to-prod:
    needs: build-and-push
    runs-on: ubuntu-latest
    name: Deploy to Production Server

    steps:
      - name: Deploy to Server via SSH
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.PROD_DOMAIN }}
          username: ${{ secrets.PROD_SERVER_USER }}
          key: ${{ secrets.PROD_SSH_KEY }}
          script: |
            set -e # Прерывать выполнение при любой ошибке

            # --- Шаг 1: Подготовка и клонирование/обновление ---
            export PROJECT_DIR="/home/${{ secrets.PROD_SERVER_USER }}/blog-flask"
            if [ ! -d "$PROJECT_DIR" ]; then
              echo "✅ Cloning repository for the first time..."
              git clone https://github.com/sttewiee/blog-flask.git "$PROJECT_DIR"
            else
              echo "✅ Updating repository..."
              cd "$PROJECT_DIR"
              git pull
            fi
            
            cd "$PROJECT_DIR/blog-flask-prod"

            # --- Шаг 2: Создание .env файла ---
            echo "✅ Creating/updating .env file..."
            cat <<EOF > .env
            POSTGRES_USER=${{ secrets.PROD_DB_USER }}
            POSTGRES_PASSWORD=${{ secrets.PROD_DB_PASSWORD }}
            POSTGRES_DB=${{ secrets.PROD_DB_NAME }}
            FLASK_ENV=production
            DATABASE_URL=postgresql://${{ secrets.PROD_DB_USER }}:${{ secrets.PROD_DB_PASSWORD }}@db:5432/${{ secrets.PROD_DB_NAME }}
            SECRET_KEY=${{ secrets.PROD_SECRET_KEY }}
            DOMAIN=${{ secrets.PROD_DOMAIN }}
            EMAIL=${{ secrets.PROD_EMAIL }}
            EOF

            # --- Шаг 3: Скачивание образов ---
            echo "✅ Pulling latest Docker images..."
            sudo docker-compose -f docker-compose.prod.yml pull

            # --- Шаг 4: Обход бага docker-compose v1 ---
            # Всегда останавливаем и удаляем контейнеры перед запуском,
            # чтобы избежать ошибки 'KeyError: ContainerConfig'.
            echo "♻️ Stopping and removing old containers to prevent bugs..."
            sudo docker-compose -f docker-compose.prod.yml down --remove-orphans

            # --- Шаг 5: Проверка и получение SSL сертификата ---
            echo "⏳ Checking SSL certificate..."
            # Запускаем certbot в режиме 'renew'. Эта команда "умная":
            # если сертификата нет, она его получит; если он есть, она его проверит.
            # Для этого ей нужен запущенный веб-сервер на 80 порту.
            sudo docker-compose -f docker-compose.prod.yml run --rm --service-ports -d nginx
            
            echo "⏳ Attempting to obtain/renew certificate..."
            sudo docker-compose -f docker-compose.prod.yml run --rm certbot renew

            # Перезапускаем Nginx, чтобы он подхватил только что полученный/обновленный сертификат.
            echo "♻️ Restarting Nginx to apply SSL certificate..."
            sudo docker-compose -f docker-compose.prod.yml restart nginx

            # --- Шаг 6: Запуск остальных сервисов ---
            echo "������ Starting all other services..."
            sudo docker-compose -f docker-compose.prod.yml up -d --remove-orphans

            # --- Шаг 7: Умное ожидание и миграция ---
            echo "⏳ Waiting for database to be ready..."
            # Цикл, который ждет, пока база данных не будет готова принимать подключения.
            until sudo docker-compose -f docker-compose.prod.yml exec db pg_isready --username=${{ secrets.PROD_DB_USER }} --quiet; do
                echo "Database is unavailable - sleeping"
                sleep 2
            done
            
            echo "✅ Database is ready! Applying migrations..."
            sudo docker-compose -f docker-compose.prod.yml exec web flask db upgrade

            echo "������ Deployment to production completed successfully!"
            echo "✅ Site is available at: https://${{ secrets.PROD_DOMAIN }}"