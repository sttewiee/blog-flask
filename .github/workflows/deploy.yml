# Этот workflow собирает образ и автоматически разворачивает его на сервере
name: Build, Push and Deploy

on:
  push:
    branches:
      - main # Запускается при пуше в main

jobs:
  # Этап сборки образа (без изменений)
  build-and-push:
    runs-on: ubuntu-latest
    name: Build and Push Docker Image
    steps:
      - uses: actions/checkout@v4
      - uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/flask-blog:latest

  # Этап автоматического развертывания
  deploy-to-prod:
    needs: build-and-push
    runs-on: ubuntu-latest
    name: Deploy to Production Server

    steps:
      - name: Deploy to Server via SSH
        uses: appleboy/ssh-action@v1.0.0
        with:
          # Подключаемся по доменному имени, а не по IP
          host: ${{ secrets.PROD_DOMAIN }}
          username: ${{ secrets.PROD_SERVER_USER }}
          key: ${{ secrets.PROD_SSH_KEY }}
          script: |
            # Переменные для удобства
            PROJECT_DIR="/home/${{ secrets.PROD_SERVER_USER }}/blog-flask"
            PROD_DIR="$PROJECT_DIR/blog-flask-prod"
            ENV_FILE="$PROD_DIR/.env"

            # 1. Клонируем или обновляем репозиторий
            if [ ! -d "$PROJECT_DIR" ]; then
              echo "✅ Cloning repository..."
              git clone https://github.com/${{ github.repository }}.git "$PROJECT_DIR"
            else
              echo "✅ Updating repository..."
              cd "$PROJECT_DIR"
              git pull
            fi

            # 2. Переходим в директорию с prod-файлами
            cd "$PROD_DIR"

            # 3. Автоматически создаем .env файл из секретов GitHub
            echo "✅ Creating/updating .env file..."
            cat <<EOF > $ENV_FILE
            # Auto-generated by GitHub Actions
            POSTGRES_USER=${{ secrets.PROD_DB_USER }}
            POSTGRES_PASSWORD=${{ secrets.PROD_DB_PASSWORD }}
            POSTGRES_DB=${{ secrets.PROD_DB_NAME }}
            FLASK_ENV=production
            DATABASE_URL=postgresql://${{ secrets.PROD_DB_USER }}:${{ secrets.PROD_DB_PASSWORD }}@db:5432/${{ secrets.PROD_DB_NAME }}
            SECRET_KEY=${{ secrets.PROD_SECRET_KEY }}
            DOMAIN=${{ secrets.PROD_DOMAIN }}
            EMAIL=${{ secrets.PROD_EMAIL }}
            EOF

            # 4. Скачиваем свежий Docker-образ
            echo "✅ Pulling latest Docker image..."
            docker-compose -f docker-compose.prod.yml pull

            # 5. Запускаем все сервисы
            echo "✅ Starting services with Docker Compose..."
            docker-compose -f docker-compose.prod.yml up -d

            # 6. Получаем SSL-сертификат (только если его еще нет)
            if [ ! -d "/etc/letsencrypt/live/${{ secrets.PROD_DOMAIN }}" ]; then
              echo "✅ Obtaining SSL certificate..."
              sleep 15 # Ждем, чтобы Nginx точно успел запуститься
              docker-compose -f docker-compose.prod.yml run --rm --entrypoint "/bin/sh -c" certbot 'certbot certonly --webroot --webroot-path /var/www/certbot -d "$DOMAIN" --email "$EMAIL" --agree-tos --no-eff-email --quiet'
              docker-compose -f docker-compose.prod.yml restart nginx
            else
              echo "✅ SSL certificate already exists. Ensuring renewal..."
              # Пытаемся обновить сертификат (он обновится, только если срок подходит к концу)
              docker-compose -f docker-compose.prod.yml run --rm --entrypoint "/bin/sh -c" certbot 'renew'
            fi

            # 7. Применяем миграции базы данных
            echo "✅ Applying database migrations..."
            docker-compose -f docker-compose.prod.yml exec web flask --app wsgi:app db upgrade

            echo "������ Deployment to production completed successfully!"