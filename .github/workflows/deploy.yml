# .github/workflows/deploy.yml

# ... (начало файла без изменений: name, on, jobs, build-and-push) ...

  deploy-to-prod:
    needs: build-and-push
    runs-on: ubuntu-latest
    name: Deploy to Production Server

    steps:
      - name: Deploy to Server via SSH
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.PROD_DOMAIN }}
          username: ${{ secrets.PROD_SERVER_USER }}
          key: ${{ secrets.PROD_SSH_KEY }}
          script: |
            # --- ШАГ 0: Подготовка переменных ---
            PROJECT_DIR="/home/${{ secrets.PROD_SERVER_USER }}/blog-flask"
            PROD_DIR="$PROJECT_DIR/blog-flask-prod"
            ENV_FILE="$PROD_DIR/.env"
            CERT_DIR="/etc/letsencrypt/live/${{ secrets.PROD_DOMAIN }}"

            # --- ШАГ 1: Клонирование или обновление репозитория ---
            if [ ! -d "$PROJECT_DIR" ]; then
              echo "✅ Cloning repository..."
              git clone https://github.com/${{ github.repository }}.git "$PROJECT_DIR"
            else
              echo "✅ Updating repository..."
              cd "$PROJECT_DIR"
              git pull
            fi
            
            # Переходим в рабочую директорию
            cd "$PROD_DIR"

            # --- ШАГ 2: Создание .env файла ---
            echo "✅ Creating/updating .env file..."
            cat <<EOF > $ENV_FILE
            # Auto-generated by GitHub Actions
            POSTGRES_USER=${{ secrets.PROD_DB_USER }}
            POSTGRES_PASSWORD=${{ secrets.PROD_DB_PASSWORD }}
            POSTGRES_DB=${{ secrets.PROD_DB_NAME }}
            FLASK_ENV=production
            DATABASE_URL=postgresql://${{ secrets.PROD_DB_USER }}:${{ secrets.PROD_DB_PASSWORD }}@db:5432/${{ secrets.PROD_DB_NAME }}
            SECRET_KEY=${{ secrets.PROD_SECRET_KEY }}
            DOMAIN=${{ secrets.PROD_DOMAIN }}
            EMAIL=${{ secrets.PROD_EMAIL }}
            EOF

            # --- ШАГ 3: Скачивание свежих образов ---
            echo "✅ Pulling latest Docker images..."
            docker-compose -f docker-compose.prod.yml pull

            # --- ШАГ 4: Логика получения SSL-сертификата (самое важное) ---
            # Проверяем, существует ли папка с сертификатом
            if ! docker-compose -f docker-compose.prod.yml run --rm --entrypoint "[ -d $CERT_DIR ]" certbot; then
              echo "������ SSL certificate not found. Attempting to obtain one..."
              
              # 1. Останавливаем все, что может занимать 80 порт
              docker-compose -f docker-compose.prod.yml down --remove-orphans
              
              # 2. Запускаем ТОЛЬКО Nginx для прохождения проверки
              docker-compose -f docker-compose.prod.yml up -d nginx

              # 3. Получаем сертификат
              echo "⏳ Obtaining certificate..."
              docker-compose -f docker-compose.prod.yml run --rm certbot certonly --webroot --webroot-path=/var/www/certbot -d "${{ secrets.PROD_DOMAIN }}" --email "${{ secrets.PROD_EMAIL }}" --agree-tos --no-eff-email --quiet

              # 4. Снова все останавливаем, чтобы применить конфиг с HTTPS
              echo "♻️ Restarting services to apply SSL..."
              docker-compose -f docker-compose.prod.yml down --remove-orphans
            else
              echo "✅ SSL certificate found. Skipping obtaining."
            fi

            # --- ШАГ 5: Финальный запуск всей системы ---
            echo "������ Starting all services..."
            docker-compose -f docker-compose.prod.yml up -d

            # --- ШАГ 6: Применение миграций базы данных ---
            echo "Applying database migrations..."
            # Даем базе немного времени на запуск после up -d
            sleep 10
            docker-compose -f docker-compose.prod.yml exec web flask --app wsgi:app db upgrade
            
            # --- ШАГ 7: Обновление сертификата (если он уже был) ---
            echo "Ensuring certificate renewal..."
            docker-compose -f docker-compose.prod.yml run --rm certbot renew

            echo "������ Deployment to production completed successfully!"