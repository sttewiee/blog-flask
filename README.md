# Корпоративная Wiki на Flask

Это проект корпоративной базы знаний (Wiki), разработанный на фреймворке Flask. Изначально это был простой блог, который был полностью переработан для поддержки структурированного хранения информации, версионирования, разграничения прав доступа и полнотекстового поиска.

Проект настроен для автоматического развертывания в Google Kubernetes Engine (GKE) с использованием CI/CD пайплайнов на GitHub Actions.

## 🚀 Основные возможности

*   **Пространства (Spaces)**: Статьи сгруппированы по пространствам (например, "Разработка", "Маркетинг", "HR").
*   **Иерархия статей**: Статьи можно вкладывать друг в друга, создавая древовидную структуру знаний.
*   **Версионирование**: Каждое изменение статьи сохраняется. Можно просмотреть историю правок, чтобы увидеть, кто и когда вносил изменения.
*   **Система ролей**:
    *   `ADMIN`: полный доступ ко всем статьям.
    *   `EDITOR`: может создавать и редактировать собственные статьи.
    *   `VIEWER`: (зарезервировано) может только просматривать статьи.
*   **Полнотекстовый поиск**: Быстрый, регистронезависимый поиск по заголовкам и содержимому всех статей.
*   **Готовность к DevOps**: Проект включает Docker-конфигурацию, K8s-манифесты и продвинутые CI/CD пайплайны.

## 🛠️ Технологический стек

*   **Бэкенд**: Python 3.11, Flask, SQLAlchemy
*   **База данных**: PostgreSQL (для локальной разработки), SQLite (в K8s)
*   **Контейнеризация**: Docker, Docker Compose
*   **Оркестрация**: Kubernetes (GKE)
*   **CI/CD**: GitHub Actions
*   **Мониторинг**: Prometheus, Grafana

## 📦 Запуск для локальной разработки

Для запуска проекта на локальной машине вам понадобится Docker и Docker Compose.

1.  **Склонируйте репозиторий:**
    ```bash
    git clone https://github.com/sttewiee/blog-flask.git
    cd blog-flask
    ```

2.  **Запустите приложение с помощью Docker Compose:**
    ```bash
    docker compose up --build
    ```

3.  **Откройте приложение в браузере:**
    Перейдите по адресу [http://localhost:5000](http://localhost:5000).

## ✨ CI/CD Пайплайны

В проекте настроено два пайплайна GitHub Actions:

1.  **`ci.yml` (Continuous Integration)**:
    *   **Цель**: Проверка качества и безопасности кода при каждом коммите и Pull Request.
    *   **Этапы**:
        1.  **Linting**: Проверка стиля кода с помощью `flake8`.
        2.  **Testing**: Запуск юнит-тестов (`pytest`) и отправка отчета о покрытии кода на Codecov.
        3.  **Security Scan**: Сканирование Docker-образа на известные уязвимости (CVE) с помощью `Trivy`.

2.  **`cicd-pipeline.yml` (Continuous Deployment)**:
    *   **Цель**: Сборка, пуш Docker-образа и развертывание приложения в Kubernetes.
    *   **Триггеры**:
        *   При пуше в ветку `dev` происходит развертывание в **Development** окружение.
        *   При пуше в ветку `main` происходит развертывание в **Production** окружение.
    *   **Этапы**:
        1.  Сборка и тегирование Docker-образа.
        2.  Публикация образа в Google Artifact Registry.
        3.  Применение K8s-манифестов и обновление версии приложения в соответствующем окружении (dev или prod).

## 🔑 Учетные данные по умолчанию

При первом запуске приложения автоматически создается пользователь-администратор:
*   **Логин**: `admin`
*   **Пароль**: `admin` (пароль по умолчанию, если не установлена переменная окружения `ADMIN_PASSWORD`)

Этот пользователь имеет полные права на создание, редактирование и удаление любых статей в системе.
